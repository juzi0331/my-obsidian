
# 🐍 Python 基础：比较运算符

> [!ABSTRACT] Python 基础：比较运算符
比较运算符用于比较两个值之间的关系，其运算结果始终为**布尔值**（`True` 或 `False`）。

### 1. 核心运算符手册

假设有变量 $a = 10$，$b = 20$：

|**运算符**|**描述**|**示例**|**结果 (基于 a=10,b=20)**|
|---|---|---|---|
|**`==`**|**等于**：判断左右两边是否相等|`a == b`|`False`|
|**`!=`**|**不等于**：判断左右两边是否不相等|`a != b`|`True`|
|**`>`**|**大于**：左边是否大于右边|`a > b`|`False`|
|**`<`**|**小于**：左边是否小于右边|`a < b`|`True`|
|**`>=`**|**大于等于**：左边是否大于或等于右边|`a >= b`|`False`|
|**`<=`**|**小于等于**：左边是否小于或等于右边|`a <= b`|`True`|

---

### 2. 代码实战演练

在 Python 中，你可以直接打印比较表达式的结果来查看布尔值：

```Python
a = 10
b = 20

print("a 是否等于 b:", a == b)      # 结果: False
print("a 是否不等于 b:", a != b)    # 结果: True
print("a 是否大于 b:", a > b)       # 结果: False
print("a 是否小于 b:", a < b)       # 结果: True
```

---

### 💡 核心要点与易错点

1. **`=` 与 `==` 的区别 (极其重要)**：
    - **`=`**：是**赋值**运算符，用于给变量设值（如 `a = 10`）。
    - **`==`**：是**比较**运算符，用于判断两者是否相等（如 `a == b`）。
2. **运算结果**：
    - 比较运算的返回类型永远是 `bool`（布尔型），即只有 `True`（真）或 `False`（假）两种可能。
3. **字符串比较**：
    - 比较运算符同样适用于字符串，它是按照字符的 **ASCII/Unicode 编码**顺序逐位进行比较的。
4. **链式比较**：
        - Python 支持类似数学的写法，例如 `5 < x < 10`，这在其他编程语言中通常需要用逻辑运算符连接。

---

![image.png](https://picgo.xfc49.cn/python/20251210123252115.png)