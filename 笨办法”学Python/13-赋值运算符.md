
# 🐍 Python 基础：赋值运算符

> [!ABSTRACT] Python 基础：赋值运算符
赋值运算符用于将右侧的值（或计算结果）存入左侧的变量中。

### 1. 基本赋值运算符

- **符号**：`=`
- **作用**：将 `=` 右边的值赋给左边的变量。
- **示例**：`num = 85`。
---
### 2. 复合赋值运算符

复合赋值运算符将**算术运算**与**赋值**合二为一。它先对变量进行指定的数学运算，然后再将结果重新赋给该变量。

|**运算符**|**名称**|**等价表达式**|**代码实战 (初始 num=85)**|
|---|---|---|---|
|**`+=`**|**加赋值**|$num = num + 10$|`num += 10` $\rightarrow$ $95$|
|**`-=`**|**减赋值**|$num = num - 10$|`num -= 10` $\rightarrow$ $85$|
|**`*=`**|**乘赋值**|$num = num \times 10$|`num *= 10` $\rightarrow$ $850$|
|**`/=`**|**除赋值**|$num = num \div 10$|`num /= 10` $\rightarrow$ $85.0$|
|**`//=`**|**整除赋值**|$num = num // 10$|`num //= 10` $\rightarrow$ $8.0$|
|**`%=`**|**取余赋值**|$num = num \pmod 3$|`num %= 3` $\rightarrow$ $2.0$|
|**`**=`**|**幂赋值**|$num = num^{3}$|`num **= 3` $\rightarrow$ $8.0$|

---

### 3. 优化后的代码示例

以下是修正了拼写错误并添加了清晰注释的练习代码：

```Python
# 初始化变量
num = 85

# 1. 加赋值
num += 10   # 等价于 num = num + 10
print(f"num += 10 后, num = {num}")   # 结果: 95

# 2. 减赋值
num -= 10   # 等价于 num = num - 10
print(f"num -= 10 后, num = {num}")   # 结果: 85

# 3. 乘赋值
num *= 10   # 等价于 num = num * 10
print(f"num *= 10 后, num = {num}")   # 结果: 850

# 4. 除赋值 (注意：除法运算结果会自动转为浮点数)
num /= 10   # 等价于 num = num / 10
print(f"num /= 10 后, num = {num}")   # 结果: 85.0

# 5. 整除赋值
num //= 10  # 等价于 num = num // 10
print(f"num //= 10 后, num = {num}")  # 结果: 8.0

# 6. 取余赋值
num %= 3    # 等价于 num = num % 3
print(f"num %= 3 后, num = {num}")    # 结果: 2.0

# 7. 幂赋值
num **= 3   # 等价于 num = num ** 3
print(f"num **= 3 后, num = {num}")   # 结果: 8.0
```

---

### 💡 核心要点

1. **运算顺序**：复合赋值运算符总是**先计算右侧的表达式**，再进行赋值。
    - 例如：`a *= b + c` 等价于 `a = a * (b + c)`。
2. **数据类型自动转换**：使用 `/=` 运算符后，即使能整除，变量的类型也会变成浮点数 (`float`)。
3. **变量必须先定义**：由于复合赋值需要引用变量当前的值（如 `num += 1` 需要知道 `num` 是多少），因此**必须先给变量赋初始值**，否则会报错。
---

![image.png](https://picgo.xfc49.cn/python/20251210120341781.png)
