
# 📜 Python 数据容器：列表 (List) 完全指南

> [!ABSTRACT] Python 数据容器：列表 (List) 
> 列表 (List) 是 Python 中最基本且最强大的数据容器。它像一个可以无限扩展的“收纳盒”，允许你按照顺序存放多个不同类型的元素。

---

## 1. 基础认知：列表是什么？

### 📝 定义语法

使用方括号 `[]` 包裹元素，元素之间用逗号 `,` 分隔。

```Python
# 列表名称 = [元素1, 元素2, 元素3]
s = [1, 2, 3, 4, "Hello", True]  # 可以存放数字、字符串、布尔值
```

### ✨ 核心四大特点

1. **有序性**：元素在容器内按特定顺序排列。
2. **多样性**：同一个列表可以存储不同类型的元素。
3. **可重复**：允许存在完全相同的元素。
4. **可修改**：可以随时增加、删除或修改列表中的内容。

---
## 2. 元素定位：下标索引 (Indexing)

列表是**序列类型**，意味着每个元素都有自己的“编号”。

- **正向索引**：从前向后，从 **0** 开始计数。
- **反向索引**：从后向前，从 **-1** 开始计数。

```Python
s = [1, 2, 3, 4, 5]
print(s[0])   # 输出 1 (第一个)
print(s[-1])  # 输出 5 (最后一个)
```

---

## 3. 核心技巧：切片操作 (Slicing)

切片是从现有列表中截取出一部分，生成一个新列表的操作。

> [!IMPORTANT] 语法格式
> 
> 列表[起始索引 : 结束索引 : 步长]
> 
> - **起始索引**：包含。
>     
> - **结束索引**：**不包含**（开区间）。
>     
> - **步长**：每次跳过的元素个数，默认为 1。
>     

```Python
s = ["A", "C", "E", "B", "D", "E", "G"]
print(s[0:5:1]) # 结果: ['A', 'C', 'E', 'B', 'D']
print(s[0:5:2]) # 结果: ['A', 'E', 'D'] (每隔一个取一个)
```

---

## 4. 动态管理：列表常用方法

掌握这些方法，你就能像管理数据库一样管理列表：

|**操作分类**|**方法名称**|**代码示例**|**功能说明**|
|---|---|---|---|
|**增加**|`append()`|`s.append(10)`|在列表**尾部**追加一个元素。|
||`insert()`|`s.insert(0, 13)`|在指定索引**之前**插入元素。|
|**删除**|`remove()`|`s.remove(9)`|移除列表中**第一个**匹配的特定值。|
||`pop()`|`s.pop(0)`|删除并**返回**指定索引位置的元素。|
||`del`|`del s[0]`|关键字删除，直接干掉指定位置元素。|
|**修改**|索引赋值|`s[0] = "New"`|直接通过下标修改内容。|
|**查询**|`len()`|`len(s)`|获取列表的总长度（元素个数）。|
||`sum()`|`sum(s)`|对数值列表求总和。|
|**整理**|`sort()`|`s.sort()`|对列表进行**升序**排列（类型需一致）。|
||`reverse()`|`s.reverse()`|将列表顺序**反转**。|

---

## 5. 高级进阶：列表推导式

这是 Python 程序员的“优雅”武器，可以用一行代码生成列表。

- **基础模式**：`[表达式 for 变量 in 序列]`
- **带条件模式**：`[表达式 for 变量 in 序列 if 条件]`

```Python
# 案例：生成 1-20 的平方列表
num_list = [i**2 for i in range(1, 21)]

# 案例：从列表中提取偶数的平方
nums = [12, 32, 45, 80]
even_squares = [i**2 for i in nums if i % 2 == 0]
```

---

## 💡 实战案例总结

### 📊 案例 1：用户输入数据处理

- **逻辑**：循环获取输入 -> `append` 到列表 -> `sort` 排序 -> 索引 `[0]` 取最小，`[-1]` 取最大。

### 🔗 案例 2：列表合并与去重

- **合并**：可以使用 `list1 + list2` 或扩展解包 `[*list1, *list2]`。
- **去重逻辑**：创建一个空列表，遍历旧列表，如果 `num not in new_list` 则添加。

---
> [!TIP] Obsidian 使用小建议
> 
> - 点击内部链接查看相关章节：[[22-数据容器-字符串str]]、[[23数据容器-元组tuple]]。
>     
> - 建议将此笔记放在 `Python/DataStructures` 文件夹下，方便检索。
>     

---
![image.png](https://eo-picgo.xfc49.cn/python/20251217141424573.png)