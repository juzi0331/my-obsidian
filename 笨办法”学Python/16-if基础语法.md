
# 🐍 Python 流程控制：if 条件判断

> [!ABSTRACT] Python 流程控制：if 条件判断
> Python 通过缩进来表示代码块的分级。

### 1. 基础语法结构

#### ① 单分支结构 (`if`)

当满足特定条件时，执行后续操作 1。

```Python
if 条件:
    条件成立时执行的代码 [cite: 1]
```

- **实战案例**：高考分数判断。如果分数 `> 680`，打印“去读清华” 2。
#### ② 双分支结构 (`if-else`)

满足条件执行操作 A，否则执行操作 B 3。

```Python
if 条件:
    条件成立执行 [cite: 1]
else:
    条件不成立执行 [cite: 1]
```

- **实战案例**：模拟登录系统。判断用户输入的账号密码是否与预设一致 4。

#### ③ 多分支结构 (`if-elif-else`)

用于处理多个并列的条件判断 5。

```Python
if 条件1:
    执行1
elif 条件2:
    执行2
else:
    都不满足时执行
```

---

### 2. 核心逻辑练习总结

通过你的练习，我们可以总结出几个关键的逻辑应用场景：

|**场景**|**逻辑运算符**|**核心代码逻辑**|
|---|---|---|
|**数值比较**|`>`|`if score > 680:` 6|
|**身份验证**|`and`|`if account == ok_account and password == ok_password:` 7|
|**复杂运算 (闰年)**|`and`, `or`, `%`|`(year % 4 == 0 and year % 100 != 0) or (year % 400 == 0)` 8|
|**多用户登录**|`elif`|分别判断 `admin`、`root` 或 `zhangsan` 的凭据 9|

---

### 3. 代码优化建议

#### 💡 逻辑简化：多账号登录

在你的多账号登录练习中，每个 `elif` 块都重复写了 `print("登陆成功")` 10。我们可以利用 `or` 运算符将逻辑合并，提高代码的可维护性：

```Python
# 优化后的多账号判断
user = input("请输入用户名:")
password = input("请输入密码:")

# 将符合条件的组合放在一起判断
is_admin = (user == "admin" and password == "666888")
is_root = (user == "root" and password == "888999")
is_zhangsan = (user == "zhangsan" and password == "123456")

if is_admin or is_root or is_zhangsan:
    print("登录成功")
else:
    print("用户名或密码错误")
```

#### 🛠️ 注意事项

1. **缩进**：Python 严格依赖缩进（通常为 4 个空格），缩进不一致会导致 `IndentationError`。
2. **冒号**：每个 `if`、`elif`、`else` 语句末尾必须带冒号 `:` 11。
3. **数据类型转换**：使用 `input()` 获取的输入默认为字符串。若进行数值比较（如年份或分数），需使用 `int()` 或 `float()` 转换 12。

---

![image.png](https://picgo.xfc49.cn/python/20251210142545288.png)

---

![image.png](https://picgo.xfc49.cn/python/20251211102450118.png)

---

![image.png](https://picgo.xfc49.cn/python/20251211103746981.png)
