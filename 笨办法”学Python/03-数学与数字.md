
# 🐍 Python 基础：数学与数字

> [!ABSTRACT] Python 基础：数学与数字
在 Python 中，数字处理不仅包括基础的四则运算，还涉及逻辑判断产生的布尔值结果。

### 1. 算术运算符 (Arithmetic Operators)

这些运算符用于执行数学计算。

|**符号**|**名称**|**描述**|**示例**|
|---|---|---|---|
|**`+`**|**加**|加法运算|`3 + 2` $\rightarrow$ `5`|
|**`-`**|**减**|减法运算|`5 - 7` $\rightarrow$ `-2`|
|**`*`**|**乘**|乘法运算|`25 * 3`|
|**`/`**|**除**|浮点除法（结果带小数）|`30 / 6` $\rightarrow$ `5.0`|
|**`%`**|**取余**|返回除法的余数|`4 % 2` $\rightarrow$ `0`|

---

### 2. 比较运算符 (Comparison Operators)

比较运算符用于对比两个数值，其结果为**布尔值**（`True` 或 `False`）。

|**符号**|**名称**|**示例**|**结果**|
|---|---|---|---|
|**`>`**|**大于**|`5 > -2`|`True`|
|**`<`**|**小于**|`3 + 2 < 5 - 7`|`False`|
|**`>=`**|**大于等于**|`5 >= -2`|`True`|
|**`<=`**|**小于等于**|`5 <= -2`|`False`|

---

### 3. `print` 函数的混合输出逻辑

在 `print()` 中，区分“内容”与“计算”的关键在于**引号**：
- **引号内 (`"..."`)**：被视为**字符串**，原样打印其内容。
- **引号外**：被视为**数学表达式**，Python 会先进行计算，然后打印计算出的结果。

#### 🔍 实战演练

```Python
# 双引号的内容被打印，双引号外的进行计算
print("母鸡", 25 + 30 / 6)  # 输出：母鸡 30.0

# 如果直接放表达式，则输出布尔判定结果
print("3 + 2 < 5 - 7 是对的吗?", 3 + 2 < 5 - 7) # 输出：... False
```

---

### 💡 核心要点总结

1. **运算优先级**：Python 遵循先乘除、后加减的数学原则（PEMDAS）。
2. **整数与浮点数**：即使是整数相除（如 `30 / 6`），结果也会自动转换为**浮点数**（`5.0`）。
3. **判定机制**：比较运算符不仅仅是符号，它们会返回逻辑结果。这在后续学习 `if` 条件判断时至关重要。
---

**优化说明：**
- **结构化表格**：将算术和比较运算符分开展示，方便查阅。
- **逻辑区分**：明确了引号在 `print` 函数中的作用，这是初学者最容易混淆的地方。
- **保留趣味性**：保留了您案例中“数鸡”的逻辑，使笔记不枯燥。

---

![](https://s2.loli.net/2025/12/07/Z7fQdbnJ8ruo92S.png)

