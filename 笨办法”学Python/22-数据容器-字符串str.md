
# 🔤 Python 数据容器：字符串 (String) 详解

> [!ABSTRACT] Python 数据容器：字符串 (String) 
> 字符串是 Python 中最常用的数据类型之一，用于存储文本信息。它在内存中以字符序列的形式存储，具有不可变性。

---

## 1. 核心特性

- **不可变性 (Immutable)**：一旦创建，字符串内部的字符无法被修改。
- **有序性**：每个字符都有对应的下标（索引），支持正向和反向访问。
- **可迭代性**：可以使用 `for` 循环遍历字符串中的每一个字符。

---

## 2. 下标索引与切片

### 📍 下标访问

通过元素对应的索引，可以精准获取到字符串中的某个字符。

- 正向索引：从 `0` 开始。
- 反向索引：从 `-1` 开始。

### 🔪 切片操作 (Slicing)

这是处理字符串的常用技巧，语法为 `[起始:结束:步长]`。

- **反转字符串**：使用 `[::-1]` 可以快速将字符串顺序颠倒。

---

## 3. 常用方法分类汇总

根据你的代码练习，重点掌握以下方法：

|**方法**|**功能描述**|**实战场景**|
|---|---|---|
|`count(sub)`|统计子字符串 `sub` 出现的次数。|验证邮箱中 `@` 的数量。|
|`upper()`|将字符串全部转换为大写。|规范化用户输入的字符串格式。|
|`lower()`|将字符串全部转换为小写。|忽略大小写的对比验证。|
|`strip()`|去除首尾空格。|清理用户输入的多余空格。|

---

## 4. 实战案例解析

### 📧 案例 1：基础邮箱格式验证

利用 `count()` 方法判断必要符号是否存在。

```Python
mail = input("请输入邮箱:")

# 合法逻辑：必须包含 1 个 @ 且至少包含 1 个点号
if mail.count("@") == 1 and mail.count(".") >= 1:
    print(f"{mail} 合法邮箱")
else:
    print(f"{mail} 非法邮箱")
```

### 🔄 案例 2：批量字符串逆序处理

结合**循环**、**切片**与**列表**的综合应用。

- **目标**：输入 10 个字符串，反转并转大写，存入列表。
- **核心代码逻辑**：
	1. `reversed_str = user_input[::-1]` (利用切片反转)。
	2. `upper_str = reversed_str.upper()` (转大写)。
	3. `processed_strings.append(upper_str)` (存入列表)。
---

## 💡 学习建议

> [!TIP] 技巧
> 
> 1. **不可变性演示**：尝试执行 `s = "abc"; s[0] = "A"` 会报错，这是理解字符串与列表区别的关键点。
>     
> 2. **链式调用**：字符串方法可以链式使用，例如 `user_input[::-1].upper()` 可以在一行内完成反转和大写转换。
>     

---

![image.png](https://eo-picgo.xfc49.cn/python/20251218135839323.png)

---

![image.png](https://eo-picgo.xfc49.cn/python/20251218141045128.png)

---

![image.png](https://eo-picgo.xfc49.cn/python/20251219135725717.png)